{
  "name": "beastmode-tests",
  "private": true,
  "description": "Harness de testes jsdom do BeastMode (o app em si é estático, sem build)",
  "scripts": {
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
